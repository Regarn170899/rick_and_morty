import React from 'react';
import styles from "./Logo.module.scss";

const Logo = () => {
    return (
        <div>
            <div className={styles.logo}></div>
        </div>
    );
};

export default Logo;